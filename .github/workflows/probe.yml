name: Runner probe

on:
  workflow_dispatch:

jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        runner: [windows-latest, windows-2022, ubuntu-latest]
    runs-on: ${{ matrix.runner }}
    steps:
    - name: Echo
      run: echo "runner alive on ${{ matrix.runner }}"
